<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Smartpoints_Interface.teacher"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Smartpoints_Interface"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid Padding="40" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Student naam" Grid.Row="0" Margin="0,0,0,4"/>
        <TextBox x:Name="StudentNameTextBox" Grid.Row="1" PlaceholderText="Naam student" />

        <TextBlock Text="Code" Grid.Row="2" Margin="0,8,0,4"/>
        <TextBox x:Name="CodeTextBox" Grid.Row="3" PlaceholderText="Voer code in" />

        <Grid Grid.Row="4" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Week (nummer)" Margin="0,0,0,4"/>
                <muxc:NumberBox x:Name="WeekNumberBox" Minimum="1" Maximum="52" Value="1" />
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                <TextBlock Text="Vak" Margin="0,0,0,4"/>
                <ComboBox x:Name="SubjectComboBox" PlaceholderText="Kies vak" />
            </StackPanel>
        </Grid>

        <TextBlock Text="Titel" Grid.Row="5" Margin="0,8,0,4"/>
        <TextBox x:Name="TitleTextBox" Grid.Row="6" PlaceholderText="Voer titel in"/>

        <StackPanel Orientation="Vertical" Grid.Row="7" Margin="0,12,0,0">
            <TextBlock Text="Cijfers" Margin="0,0,0,6"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <muxc:NumberBox x:Name="NewGradeNumberBox" Minimum="0" Maximum="10" SmallChange="0.1" Header="Nieuw cijfer" Width="140"/>
                <Button x:Name="AddGradeButton" Content="Voeg toe" Click="AddGrade_Click"/>
                <Button x:Name="ClearGradesButton" Content="Wis alle cijfers" Click="ClearGradesButton_Click"/>
            </StackPanel>

            <ListView x:Name="GradesListView" Margin="0,8,0,0" Height="140" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="x:Double">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                            <TextBlock Text="{x:Bind}" VerticalAlignment="Center" Width="80"/>
                            <Button Content="Verwijder" Click="RemoveGrade_Click" DataContext="{x:Bind}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,8,0,0">
                <TextBlock Text="Gemiddelde cijfer:" VerticalAlignment="Center"/>
                <TextBox x:Name="AverageTextBox" IsReadOnly="True" Width="120"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,12,0,0">
                <Button x:Name="SubmitButton" Content="Opslaan" Click="SubmitButton_Click"/>
                <Button x:Name="ClearButton" Content="Wis velden" Click="ClearButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
